//! Semantic validation tests
//!
//! These tests verify that the semantic validator correctly detects
//! errors, warnings, and validates document correctness.

mod helpers;

mod dependencies;
mod duplicates;
mod references;
mod validation;
