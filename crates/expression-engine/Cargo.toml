[package]
name = "expression-engine"
version = "0.1.0"
edition = "2021"
license.workspace = true

[dependencies]
model = { path = "../model" }
ppl-syntax = { path = "../ppl-syntax" }
bigdecimal = "0.4"
chrono = "0.4.39"
smallvec = "1.15"
tracing = "0.1"
thiserror = "2.0"
hex = "0.4"

[dev-dependencies]
# Only need engine-core for tests
engine-core = { path = "../engine-core" }
