CONNECTIONS (
    DESTINATION(POSTGRES, "postgres://user:password@localhost:5432/testdb")
);

MIGRATE(
    SOURCE(CSV, "/home/stan/repos/data/movies.csv") -> DEST(TABLE, orders) [
        SETTINGS(
            CSV_HEADER            = TRUE,
            CSV_DELIMITER         = ",",
            BATCH_SIZE            = 10,
            CREATE_MISSING_TABLES = TRUE
        )
    ]
);