CONNECTIONS(
    SOURCE(MYSQL,  "mysql://sakila_user:qwerty123@localhost:3306/sakila"),
    DESTINATION(POSTGRES, "postgres://user:password@localhost:5432/testdb")
);
MIGRATE(
                SOURCE(TABLE, actor) -> DEST(TABLE, actor) [
                    SETTINGS(CREATE_MISSING_COLUMNS=TRUE),
                    MAP(actor_id -> acc_id, CONCAT(actor[first_name], " ", actor[last_name]) -> first_name, actor[first_name] -> acc_id)
                ]
            );