CONNECTIONS(
    SOURCE(MYSQL,  "mysql://sakila_user:qwerty123@localhost:3306/sakila"),
    DESTINATION(POSTGRES, "postgres://user:password@localhost:5432/testdb")
);
MIGRATE(
    SOURCE(CSV, "dev-fixtures/csv/customers.csv") -> DEST(TABLE, customers) [
        SETTINGS(CREATE_MISSING_COLUMNS=TRUE,CSV_HEADER=TRUE),
        MAP(
            CONCAT(customers[first_name], customers[last_name]) -> full_name
        )
    ]
);